***********
# How to use:
***********
Ensure you have packages installed (listed at top of code). Important ones are pickle and opencv2.

Load code into a python shell, preferably anaconda. Can also be run as an .exe program.

Use keyboard for input. 

The program will prompt you with possible commands and inputs.

Use the save command to create the files that will be used to hold data.
